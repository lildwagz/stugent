<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ei.eseptiyadi.aps">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.Splash1Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.DatapenilaianActivity" />
        <activity android:name=".views.InputPenilaian" />
        <activity
            android:name=".views.ActivityClass"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".views.detail_data" />
        <activity android:name=".views.data_pelajaran" />
        <activity android:name=".views.addDataSiswa" />
        <activity android:name=".views.dasboard">

        </activity>
    </application>

</manifest>